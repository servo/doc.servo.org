initSidebarItems({"enum":[["ErrorKind","The kind of error that can be produced during a serialization or deserialization."]],"mod":[["ipc",""],["platform",""],["router","Routers allow converting IPC channels to crossbeam channels. The RouterProxy provides various methods to register `IpcReceiver<T>`s. The router will then either call the appropriate callback or route the message to a crossbeam `Sender<T>` or `Receiver<T>`. You should use the global `ROUTER` to access the `RouterProxy` methods (via `ROUTER`’s `Deref` for `RouterProxy`."]],"type":[["Error","An error that can be produced during (de)serializing."]]});